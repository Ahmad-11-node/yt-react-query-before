module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:react/recommended',
    'plugin:react/jsx-runtime',
    'plugin:react-hooks/recommended',
  ],
  // src/components/ui is generated by the shadcn CLI and is overwritten
  // whenever a component is re-added, so linting it is noise.
  ignorePatterns: ['dist', '.eslintrc.cjs', 'src/components/ui'],
  parserOptions: { ecmaVersion: 'latest', sourceType: 'module' },
  settings: { react: { version: 'detect' } },
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
    // This codebase doesn't use the prop-types package; component contracts
    // live in the code rather than in runtime validators.
    'react/prop-types': 'off',
  },
  overrides: [
    {
      // The router maps paths to lazy() imports. Those consts look like
      // components to the rule, but they're route definitions — the pages
      // themselves live in src/routes and fast-refresh normally.
      files: ['src/router.jsx'],
      rules: { 'react-refresh/only-export-components': 'off' },
    },
  ],
}
